language: python
python:
  - "2.7"
#  - "3.6"
#  - "3.7"
# command to install dependencies
install:
#  - pip install -r requirements.txt #if all requirments in txt file
#  - pip install numpy
#  - pip install scipy
  -pip install codecov
# command to run tests
virtualenv:
  system_site_packages: true
script:
  - coverage run test.py

after_success:
  - codecov

CODECOV_TOKEN="754ec358-2c9e-4f15-8123-b44041c1f09b"
